# API Test Framework

A lightweight API test framework scaffold suitable for 1–2 people.

How to run tests:

- Create a virtualenv and install requirements: python -m pip install -r requirements.txt
- Run smoke tests: python run.py -e test -m smoke

See `docs/run.md` for more details.

